# ============================================================
# SignaTurk — TID İletişim Sistemi
# Python 3.10+ gerektirir
# ============================================================

# === Derin Öğrenme ===
tensorflow>=2.16.0          # M1/M2/M4 için: tensorflow-macos + tensorflow-metal
numpy>=1.24.0

# === El Takibi & Görüntü ===
mediapipe>=0.10.0
opencv-python>=4.8.0

# === Ses → Metin (STT) ===
faster-whisper>=1.0.0
RealtimeSTT>=0.2.0
sounddevice>=0.4.6
soundfile>=0.12.1
PyAudio>=0.2.14
webrtcvad>=2.0.10

# === LLM (Groq API) ===
groq>=0.9.0

# === Web Sunucu ===
flask>=3.0.0
flask-socketio>=5.3.0

# === Veri & Değerlendirme ===
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0
tqdm>=4.65.0

# ============================================================
# Apple Silicon (M1/M2/M4) kurulum notu:
#   pip install tensorflow-macos tensorflow-metal
#   pip install -r requirements.txt
#
# NVIDIA GPU kurulum notu:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt
# ============================================================
